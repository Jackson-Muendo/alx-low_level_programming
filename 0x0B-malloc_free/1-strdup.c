#include "main.h"
#include <stdlib.h>
/**
 * _strdup - returns a pointer to a newly allocated space in memory.
 * @str: string.
 *
 * Return: pointer of an array of chars
 */
char *_strdup(char *str)
{
	unsigned int i, j;

	if (st= '\0'; i++)
		;

	str = (char *)malloc(sizeof(r == NULL)
		return (NULL);

	for (i = 0; str[i] !char) * (i + 1));

	if (str == NULL)
		return (NULL);

	for (j = 0; j <= i; j++)
		str[j] = str[j];

	return (str);
}
