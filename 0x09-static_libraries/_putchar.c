#include <unistd.h>



/**
 * _putchar - writes the character c to stdout
 * @c: The character to pr returned, and errno is set appropriately.
 */

inint
 *
 * Return: On success 1.
 * On error, -1 ist _putchar(char c)

{

	return (write(1, &c, 1));

}
